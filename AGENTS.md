# AGENTS.md — دليل الوكلاء

> هذا الملف يوجه وكلاء الذكاء الاصطناعي لفهم بنية الموقع وقواعد التعامل معه.

## الموقع

- **الاسم**: AEO Lab — موقع AEO التجريبي
- **اللغة**: العربية (RTL)
- **الاستضافة**: GitHub Pages
- **الرابط**: https://mohammed74a.github.io/aeo-test-site/

## البنية

- HTML ثابت 100% — بدون JavaScript، بدون أطر عمل.
- التصميم: ملف CSS واحد مشترك `style.css`.
- التنقل: شريط علوي ثابت في كل الصفحات (5 روابط).
- المحتوى: عربي بالكامل، اتجاه RTL.

## الصفحات

| الصفحة | المسار | الغرض |
|---|---|---|
| الرئيسية | `/` | نظرة عامة + إحصائيات + أحدث المقالات |
| المدونة | `/blog.html` | فهرس المقالات |
| الأسئلة الشائعة | `/faq.html` | 6 أسئلة (FAQ تفاعلي بدون JS) |
| عن الموقع | `/about.html` | الأهداف والبنية |
| تواصل | `/contact.html` | قنوات التواصل |
| دليل AEO | `/blog/aeo-guide.html` | مقال شامل عن AEO |
| دليل llms.txt | `/blog/llms-txt-guide.html` | مقال عن llms.txt |
| 404 | `/404.html` | صفحة الخطأ |

## ملفات AEO

- `llms.txt` — خريطة الموقع للذكاء الاصطناعي (روابط مطلقة).
- `llms-full.txt` — المحتوى الكامل للموقع.
- `robots.txt` — سياسة الزحف لـ 11 روبوت AI.
- `sitemap.xml` — خريطة الموقع (11 عنواناً).
- `agent-permissions.json` — إعلان القدرات (قراءة فقط، لا تنفيذ).

## قواعد التعديل

1. حافظ على HTML ثابت بدون JavaScript.
2. حافظ على ميزانية التوكنات: كل صفحة أقل من 300 توكن.
3. عند إضافة صفحة: حدّث `llms.txt` و`llms-full.txt` و`sitemap.xml` معاً.
4. استخدم روابط مطلقة في ملفات AEO (ليست نسبية).
5. أضف Schema JSON-LD المناسب لكل صفحة جديدة.
6. حافظ على اللغة العربية واتجاه RTL.